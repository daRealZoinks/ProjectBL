using UnityEngine;

namespace Weapons
{
    public class Firepoint : MonoBehaviour
    {
        private void Update()
        {
            // run a raycast from the virtual camera going forward
            // whatever you hit, point the firepoint at it
            // if you hit nothing, point the firepoint forward
        }
    }
}